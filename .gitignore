*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build  → committed so the site works on hosts without Node (cPanel)
/public/hot
/public/storage
/storage/*.key
/storage/pail
# /vendor  → committed for shared hosting (cPanel) without Composer/SSH
Homestead.json
Homestead.yaml
Thumbs.db
